// Custom JS for Django admin
window.addEventListener("DOMContentLoaded", function () {
  // Example: Highlight active menu item
  const activeMenu = document.querySelector(".model-admin");
  if (activeMenu) {
    activeMenu.classList.add("bg-indigo-100");
  }
  // Add more JS enhancements here
});
